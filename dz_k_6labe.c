#include <stdio.h>
#include <locale.h>

//15 вариант
main()
{
	setlocale(LC_ALL, "RUS");
	int x, y;
	puts("Введите значение координаты по ОХ...");
	scanf_s("%d", &x);
	puts("Введите значение координаты по ОУ...");
	scanf_s("%d", &y);
	if (((x >= 2) && (x <= 6)) && ((y >= 4) && (y <= 6)))
		printf("Точка с координатами (%d,%d) принадлежит области", x, y);
	else
		printf("Точка с координатами (%d,%d) не принадлежит области", x, y);
}